/** @type {import('next').NextConfig} */
const nextConfig = {
  // Отключаем кэш роутера для динамических и статичных страниц: без этого
  // Next.js может при "← Назад" показать старую версию страницы (например,
  // автомобиль без только что назначенного собственника), пока не пройдёт
  // время кэша. Проект целиком построен на живых данных из Supabase, так
  // что актуальность важнее небольшой экономии на повторных запросах.
  experimental: {
    staleTimes: {
      dynamic: 0,
      static: 30, // минимум, который допускает Next.js
    },
  },
};

export default nextConfig;
